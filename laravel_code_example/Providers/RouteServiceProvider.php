<?php

namespace App\Providers;

use App\Models\Cabinet\CProfile;
use Illuminate\Foundation\Support\Providers\RouteServiceProvider as ServiceProvider;
use Illuminate\Support\Facades\Route;

class RouteServiceProvider extends ServiceProvider
{
    /**
     * This namespace is applied to your controller routes.
     *
     * In addition, it is set as the URL generator's root namespace.
     *
     * @var string
     */
    protected $namespace = 'App\Http\Controllers';

    /**
     * The path to the "home" route for your application.
     *
     * @var string
     */
    public const HOME = '/cabinet/wallets';
    public const BACKOFFICE_HOME = '/backoffice/profiles?type=' . CProfile::TYPE_INDIVIDUAL;

    /**
     * Define your route model bindings, pattern filters, etc.
     *
     * @return void
     */
    public function boot()
    {
        //

        parent::boot();
    }

    /**
     * Define the routes for the application.
     *
     * @return void
     */
    public function map()
    {
        $this->mapApiRoutes();

        $this->mapWebRoutes();

        Route::prefix('cabinet')
            ->middleware('web')
            ->namespace($this->namespace)
            ->group(base_path('routes/cabinet-web.php'));

        Route::prefix('ajax')
            ->middleware('web')
            ->namespace($this->namespace . '\Cabinet\API\v1')
            ->group(base_path('routes/cabinet-api-v1.php'));

        Route::prefix('webhook')
            ->namespace($this->namespace . '\Webhook')
            ->group(base_path('routes/webhook.php'));
    }

    /**
     * Define the "web" routes for the application.
     *
     * These routes all receive session state, CSRF protection, etc.
     *
     * @return void
     */
    protected function mapWebRoutes()
    {
        Route::middleware('web')
            ->namespace($this->namespace)
            ->group(base_path('routes/web.php'));
    }

    /**
     * Define the "api" routes for the application.
     *
     * These routes are typically stateless.
     *
     * @return void
     */
    protected function mapApiRoutes()
    {
        Route::prefix('api')
            ->middleware('api')
            ->namespace($this->namespace)
            ->group(base_path('routes/api.php'));
    }
}
